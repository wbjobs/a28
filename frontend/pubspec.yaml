name: video_understanding_frontend
description: Multi-modal video understanding web application built with Flutter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.1.1
  web: ^0.5.0
  universal_html: ^2.2.4
  file_picker: ^6.1.1
  fl_chart: ^0.65.0
  intl: ^0.18.1
  uuid: ^4.2.1
  video_player: ^2.8.1
  chewie: ^1.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
